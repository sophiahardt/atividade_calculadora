function soma() {
    let {n1, n2} = guardar_valores();
    (n1 + n2);
}

function subtracao() {
    let {n1, n2} = guardar_valores();
    (n1 - n2);
}

function multiplicacao() {
    let {n1, n2} = guardar_valores();
    (n1 * n2);
}

function divisao() {
    let {n1, n2} = guardar_valores();
    (n1 / n2);
}